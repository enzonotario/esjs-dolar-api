importar { calcularDolarPorCasa } desde '@/acciones/consulta/calcularDolarPorCasa.esjs'

exportar porDefecto asincrono (c) => {
  const dolar = esperar calcularDolarPorCasa('tarjeta')

  si (!dolar) {
    retornar c.notFound()
  }

  retornar c.json(dolar)
}
