importar { monedasSoportadas, monedaPorDefecto } desde '@/constantes.esjs'

exportar funcion validarMoneda(moneda) {
  si (!monedasSoportadas.incluye(moneda) && moneda !== monedaPorDefecto) {
    retornar falso
  }

  retornar verdadero
}
