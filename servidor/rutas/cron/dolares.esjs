importar { guardarDolares } desde '@/acciones/guardado/guardarDolares.esjs'

exportar porDefecto asincrono funcion(req, res) {
	const dolares = esperar guardarDolares()

	res.json({
		estado: 'Correcto',
	})
}
