importar { analizar } desde 'date-fns'

exportar funcion obtenerFechaActualizacion($) {
  const texto = $('.update').text().dividir('Actualizado el')[1].recortarEspacios()
  retornar analizar(texto + ' -03', 'dd/MM/yy hh:mm aa x', crear Fecha())
}
