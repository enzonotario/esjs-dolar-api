exportar funcion interpretarVariacion(valor) {
  si (!valor || valor === 'S/C') {
    retornar nulo
  }

  const resultado = Numero(valor.reemplazar('%', '').reemplazar(',', '.'))
  retornar isNaN(resultado) ? nulo : resultado
}
