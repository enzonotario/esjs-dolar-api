exportar funcion noQueryParams() {
	retornar (peticion, respuesta, siguiente) => {
		si (peticion.url.incluye('?')) {
			// Redirigir a la misma URL sin parámetros de consulta
			respuesta.redirect(peticion.originalUrl.dividir('?')[0])
			retornar
		}

		siguiente()
	}
}
